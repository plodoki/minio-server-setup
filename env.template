# MinIO Configuration Template
# Copy this file to .env and update the values

# MinIO Root Credentials
# These are the admin credentials for MinIO
MINIO_ROOT_USER=admin
MINIO_ROOT_PASSWORD=change-this-secure-password

# Local Mount Path
# Path on the host system where MinIO will store data
# For Raspberry Pi, this could be an external drive mount point
# Example: /mnt/external-drive/minio-data or /home/pi/minio-data
LOCAL_MOUNT=/home/pi/minio-data

# Optional: Custom domain/hostname for certificate
# If you want to access MinIO via a custom domain, set it here
# This will be used during certificate generation
# CUSTOM_DOMAIN=minio.local

# Optional: Additional IP addresses for certificate
# Comma-separated list of additional IP addresses
# ADDITIONAL_IPS=192.168.1.100,10.0.0.50 